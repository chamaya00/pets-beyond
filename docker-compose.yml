version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: beyond
      POSTGRES_USER: beyond
      POSTGRES_PASSWORD: beyond
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
